# vereinsheim monorepo — pnpm config
# Default isolated node_modules behalten → pnpm-Strenge, keine Phantom-Deps (ADR-015).
# Engines (node >=24) hart prüfen, damit lokal/VPS dieselbe Node-Major nutzen.
engine-strict=true
