{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "dev (Docker Compose)",
      "runtimeExecutable": "docker",
      "runtimeArgs": ["compose", "-f", "docker-compose.dev.yml", "up", "--watch"],
      "port": 3000
    }
  ]
}
