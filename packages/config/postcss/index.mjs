// Geteilte PostCSS-Config (Tailwind v4). Das Plugin "@tailwindcss/postcss" wird
// vom App-Kontext aufgelöst (jede App deklariert es als devDependency).
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
}

export default config
